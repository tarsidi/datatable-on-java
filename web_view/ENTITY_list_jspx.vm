<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- File generated by $generator.name ( version $generator.version ) - Date $today.date("yyyy-MM-dd") ( Time $today.time ) -->

<div xmlns:jsp="http://java.sun.com/JSP/Page" xmlns:c="http://java.sun.com/jsp/jstl/core" xmlns:joda="http://www.joda.org/joda/time/tags" xmlns:s="http://www.springframework.org/tags" xmlns:form="http://www.springframework.org/tags/form" xmlns:util="urn:jsptagdir:/WEB-INF/tags/util" xmlns:display="urn:jsptagdir:/WEB-INF/tags/display" xmlns:input="urn:jsptagdir:/WEB-INF/tags/input" version="2.0">
<jsp:directive.page contentType="text/html;charset=UTF-8"/>
##--------------------------------------------------------------------------------------------------------
#set( $uncapitalizedEntityName = $fn.uncapitalize($entity.name) )
#set( $_ = "$" )
##--------------------------------------------------------------------------------------------------------
## Key fields = fields in Primary Key    
#set( $keyFields = $entity.getAttributesByCriteria( $const.KEY ) )
##--------------------------------------------------------------------------------------------------------
## Data fields = fields not in Primary Key and not in selected Links    
#set( $dataFields = $entity.getAttributesByCriteria( $const.NOT_KEY, $const.NOT_IN_SELECTED_LINKS ) )
##--------------------------------------------------------------------------------------------------------
## All fields ( ordered fiels : 1) fields in Primary Key 2) fields not in primary key    
#set( $allFields = $fn.concatLists($keyFields, $dataFields) )
##--------------------------------------------------------------------------------------------------------
## #set( $itemVariable = $fn.uncapitalize($entity.name) )
#set( $itemVariable = $fn.uncapitalize($entity.name) )
##--------------------------------------------------------------------------------------------------------
<jsp:output omit-xml-declaration="yes"/>
	<div class="container">
		<fieldset class="col-sm-12 bordure">
			<legend class="legende"><s:message code="${itemVariable}"/></legend>
		
			<util:message message="${_}{message}" messages="${_}{messages}" />
			<s:message code="filter.by"/>
			<div class="clearfix">&#160;</div>
## -----------------------------------------------------------------------------------------------
#foreach( $field in $allFields )
#set($htmlId      = "${uncapitalizedEntityName}_${field.name}" )
#set($htmlName    = $field.name )
#set($messageCode = "${uncapitalizedEntityName}.${field.name}" )
#set($maxLength   = $field.guiMaxLength )
## Is this field referenced another entity ? Is it a Foreign Key ? ( if Yes => a combobox will be used )
#* 
#set($referencedEntityTypes = $tools.referencedEntityTypes($entity,$field) )
#if( $referencedEntityTypes.size() > 0 )
#set($listOfItemsVarName = "listOf${referencedEntityTypes.get(0)}Items" ) ## If many, use the 1st one
#else
#set($listOfItemsVarName = "" ) 
#end
*# 
#if( $field.isReferencingAnotherEntity() )
#set($listOfItemsVarName = "listOf${field.referencedEntityType}Items" ) ## If many, use the 1st one
#else
#set($listOfItemsVarName = "" ) 
#end
##
#if( $field.isKeyElement() )
				<!-- PRIMARY KEY FIELD : $field.name -->
#else
				<!-- DATA FIELD : $field.name -->
#end
				<div class="form-group">
					<!-- The field label is defined in the messages file (for i18n) -->
					<label for="${htmlId}" class="col-sm-2 control-label"><s:message code="${messageCode}"/></label>
					<div class="col-sm-10">
## ------------------------------------------------ KEY ELEMENT 
#if( $field.isKeyElement() ) 
## ------------------
#if( $listOfItemsVarName != "" )
## --- Primary Key element and Foreign Key : combo box
						<form:select id="${htmlId}" path="${htmlName}" cssClass="form-control">
							<form:option value=""></form:option>
							<form:options items="${DOLLAR}{${listOfItemsVarName}}" itemValue="value" itemLabel="label" />
						</form:select>

#else
## --- Primary Key element and Not in a Foreign Key : simple field
##						<input:text id="${htmlId}" name="${htmlName}"  disabled="${DOLLAR}{mode != 'create'}" />
						<input id="${htmlId}" type="text" class="form-control" maxLength="50" style="width: 200px;"/>
#end
## ------------------
#else ## --------------------------------------- DATA ELEMENT 
## ------------------
#if( $listOfItemsVarName != "" ) ## --- Data element and Foreign Key : combo box (never disabled)
						<form:select id="${htmlId}" path="${htmlName}" cssClass="form-control">
							<form:option value=""></form:option>
							<form:options items="${DOLLAR}{${listOfItemsVarName}}" itemValue="value" itemLabel="label" />
						</form:select>
#else ## --- Data element and Not in a Foreign Key : simple field (never disabled)
#if( $field.fullType == "java.util.Date" || $field.fullType == "java.sql.Date" || $field.fullType == "java.util.Calendar" )
						<input:date id="${htmlId}"/>  <!-- DATE type -->
#elseif( $field.isLongText() )
						<textarea id="${htmlId}" /> <!-- LONG TEXT type -->
#else
						<input id="${htmlId}" type="text" class="form-control" maxLength="50" style="width: 200px;"/>
#end
#end 
## ------------------
#end 
## ------------------------------------------------

					</div>
				</div>

#end ## end of "foreach( $field in $allFields )"
## -----------------------------------------------------------------------------------------------
			<div class="form-group">
					<div class="col-sm-10">
						<button type="button" class="btn btn-primary" id="searchBtn">
							<s:message code="button.search"/>
						</button> &#x26;nbsp;
						<button type="button" class="btn btn-default" id="resetBtn">
							<s:message code="button.reset"/>
						</button>
					</div>
			</div>
			&#x26;nbsp;
			<div class="clearfix">&#160;</div>
			&#x26;nbsp;
			<table class="table table-striped" id="${itemVariable}List">
## ---TABLE HEAD ------------------------------------------------------------------
				<thead>
					<tr>
## KEY FIELDS
#foreach( $field in $keyFields )
						<th><s:message code="${itemVariable}.${field.formattedName(0)}"/></th>
#end
## DATA FIELDS
#foreach( $field in $dataFields )
						<th><s:message code="${itemVariable}.${field.formattedName(0)}"/></th>
#end
						<s:url var="url_create" value="/${itemVariable}/form"  />
						<th><a class="btn btn-info" href='${_}{url_create}'><s:message code="create"/></a></th>
					</tr>
				</thead>
## ---TABLE BODY ------------------------------------------------------------------
				<tbody>
				</tbody>
			</table>
		</fieldset>
	</div>
<s:url var="datatableSource" value="/${itemVariable}/datatableJson" />
	<script type="text/javascript">
jQuery(document).ready(function() {
	${_}("#searchBtn").click(function(){
    	var enqTable = ${_}('#${itemVariable}List').dataTable();
    	enqTable.fnDraw();
    	enqTable.fnPageChange('first');
    	return false;
    });
	
	${_}("#resetBtn").click(function(){
		
#foreach( $field in $allFields )
#set($htmlId      = "${uncapitalizedEntityName}_${field.name}" )
#set($htmlName    = $field.name )
#set($messageCode = "${uncapitalizedEntityName}.${field.name}" )
#set($maxLength   = $field.guiMaxLength )
            	${_}('#${htmlId}').val('');

#end ## end of "foreach( $field in $allFields )"
    	var enqTable = ${_}('#${itemVariable}list').dataTable();
    	enqTable.fnDraw();
    	enqTable.fnPageChange('first');
    	return false;
    });
	
	var enqTable = ${_}('#${itemVariable}List').dataTable({
		"dom": '&#x3C;l&#x3C;t&#x3E;ip&#x3E;',
		"bFilter" : true,
		"responsive" : true,
		"bProcessing": true,
        "bServerSide": true,
        "sAjaxSource": "${_}{datatableSource}",
        "fnServerParams": function (param) {
            param.push(
#foreach( $field in $allFields )
#set($htmlId      = "${uncapitalizedEntityName}_${field.name}" )
#set($htmlName    = $field.name )
#set($messageCode = "${uncapitalizedEntityName}.${field.name}" )
#set($maxLength   = $field.guiMaxLength )
            	{ "name":"${field.name}Filter", "value": ${_}('#${htmlId}').val() }#if( $foreach.hasNext ), #end

#end ## end of "foreach( $field in $allFields )"
            );
        },
        "columns": [
#foreach( $field in $allFields )
#set($htmlId      = "${uncapitalizedEntityName}_${field.name}" )
#set($htmlName    = $field.name )
#set($messageCode = "${uncapitalizedEntityName}.${field.name}" )
#set($maxLength   = $field.guiMaxLength )
            	{ "data": "${htmlName}"}#if( $foreach.hasNext ),
#else,
            { "data": "${keyFields[0].name}", 
            	"mRender" : function (data, type, oObj ) {
            		return "&#x3C;a href=\&#x22;javascript:location.href=&#x27;/${ROOT_PATH}/${uncapitalizedEntityName}/form/"+data+"';\" class=\&#x22;btn btn-info\&#x22;&#x3E;Edit&#x3C;/a&#x3E; &#x26;nbsp;"+
    					"&#x3C;a href=\&#x22;javascript:location.href=&#x27;/${ROOT_PATH}/${uncapitalizedEntityName}/delete/"+data+"';\" class=\&#x22;btn btn-info\&#x22;&#x3E;Delete&#x3C;/a&#x3E;"

                }
            }
#end
#end ## end of "foreach( $field in $allFields )"
        ]
	});
});
</script>
</div>